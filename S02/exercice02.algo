algorithme exercice2

variables
    note : réel

debut
    ecrire("donner la note : ")
    lire(note)
    si note>=0 et note<=20  alors
        ecrire("note valide")
    sinon
        ecrire("note invalide")
    FinSi
fin